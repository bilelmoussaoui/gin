install_data(
  '@0@.mime.xml'.format(application_id),
  install_dir: get_option('datadir') / 'mime'
)

install_data(
  '@0@.svg'.format(application_id),
  install_dir: get_option('datadir') / 'icons' / 'hicolor' / 'scalable' / 'mimetypes'
)
